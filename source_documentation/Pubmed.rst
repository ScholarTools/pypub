Pubmed Notes
==================

Pubmed actually provides the database for FTP downloads. At some point it might be better to have direct access to the database. Queries are currently limited to 3 per second (based on Biopython documentation).
